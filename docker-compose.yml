services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: ticket_booking_db
      POSTGRES_USER: ${DB_USERNAME:admin}
      POSTGRES_PASSWORD: ${DB_PASSWORD:admin}
